# 사전등록 — M3: 분할형 사회는 단일 context를 이기는가 (2026-08-18)

**상태: 사전등록. 데이터를 보기 전에 동결한다.** 개정은 append-only Errata로만.

## 0. 왜 이것이 G5의 재개봉이 아닌가 (`prereg_superseded`)

G5는 사회를 죽였다(society 0.4062 vs solo_ledger 0.4688, kill rule 발동). **닫힌 결정을
데이터 없이 다시 여는 것은 정지규칙 교체다.** 그러므로 무엇이 바뀌었는지 먼저 적는다.

G5가 시험한 것은 **투표형**이었다 — 같은 과제를 넘겨받아 이어서 하는 다른 agent.
그 축의 값은 이후 독립적으로 측정됐고 **없다**:

```
이종 패널 16 substrate  유효 독립표 1.75
이종 패널 20 substrate  유효 독립표 1.90
copyness (2026-08-18)   같은 모델·다른 사적기록에서 결정 발산 초과 −0.018 (단, 식별 불가)
```

이 실험이 시험하는 것은 **분할형**이다 — 서로 다른 질문에 각자의 영역으로 답하는 전문가들.
**새 관측**: 우리 코퍼스는 **11,718,417 토큰**이고 200k context의 **58.6배**다
(`docs` 층만 3.6M = 18배). 즉 단일 agent가 전체를 동시에 볼 수 없다는 것이 **가정이 아니라
측정된 사실**이며, 이는 G5에서 시험된 적 없는 제약이다.

이 구별이 중요한 이유: 분할은 단일 agent가 가질 수 있었던 정보를 나누는 게 아니라
**구조적으로 한 번에 들 수 없는 것**을 든다. A1의 *"얼어붙은 라우터가 적응형을 이긴다"*가
여기서 무력한 것도 같은 이유다 — **얼어붙은 테이블도 context에 들어가야 한다.**

## 1. 팔 (3개)

```
A  단일 agent + 전체 검색      코퍼스 전체에 대한 retrieval. 정보 접근은 제한 없음.
B  단일 agent + 최선의 압축    층별 요약 인덱스를 context에 상주. C와 무관한 구현자가 최선을 다함.
C  N 분할 전문가 + 라우터      층(organ) 단위 분할, 각자 자기 층만 상주. 라우터가 질문을 배분.
```

**분할 원칙은 지금 동결한다: `layer`(organ) 단위.** 결과를 보고 분할을 바꾸면 그건 튜닝이다.

**B가 이 설계의 전부다.** B 없이 C가 A를 이기면 우리가 발견한 것은 사회가 아니라 **압축**이다.

## 2. 과제와 오라클 — 최적화기가 만질 수 없는 곳에서

과제는 **그래프에서 기계적으로 생성**하고 정답은 **결정론적 DB 질의**로 계산한다.

```
생성기   graph에서 샘플링 (구조적, 모델이 쓰지 않음)
검증기   sqlite 질의 (결정론적, 모델이 쓰지 않음)
피험자   A/B/C의 agent들
```

세 역할이 분리되므로 루프가 자기 과제를 만들고 자기 검증기를 쓰고 자기를 채점하는 형태가
아니다(2026-07-17 지시문의 anti-reward-hacking 요건).

과제 유형(동결):
- **cross-layer 인용 추적**: X를 인용하는 문서 중 층이 다른 것과, 그 문서가 X의 무엇을 바꿨는가
- **supersede 체인**: X를 무효화한 것과 그 근거 문서
- **증거 대조**: 두 층의 서로 다른 기록이 같은 수치를 다르게 말하는 지점

## 3. 실행 전 게이트 (하나라도 실패하면 돌리지 않는다)

**G0 — 과제가 실제로 단일 context를 초과할 것.**
각 질문에 필요한 증거 문서들의 합이 `> 1 context`이고, **≥2개 층**에 걸칠 것.
초과하지 않으면 C는 정의상 무의미하고, 그 질문은 풀에서 뺀다(사후가 아니라 생성 시점에).

**G1 — 행동 분산.** 정답의 주변분포에서 **최빈값 ≤ 60%**.
*오늘 이걸 어겨서 copyness가 INDETERMINATE로 끝났다*(RETRY 90/96%). 같은 실수를 두 번
하지 않기 위해 게이트로 승격한다. 위반하면 ΔU=0은 **측정된 0이 아니라 식별 불가**다.

**G2 — 계측 캘리브레이션** (`scripts/aios_instrument.py`로 강제):
```
양성 대조  한 층만으로 답하는 질문 → C가 이겨야 한다. 아니면 라우터가 고장이다.
음성 대조  코퍼스 없이 일반지식으로 답하는 질문 → 세 팔이 같아야 한다.
           다르면 오염(모델이 이미 답을 안다)이고 측정은 코퍼스에 대한 것이 아니다.
반례       주장의 전제를 만족하는 적대 사례를 실행하고 in_scope=True를 증명할 것
```

## 4. 지표와 kill rule

주 지표: **외부 오라클이 판정한 정답률.** 짝지음(같은 질문) McNemar 정확검정.

```
C ≤ B                              ⟹ 발견한 것은 압축이지 사회가 아니다. 분할 폐기.
(C−B) 단측 95% 하한 < δ_min(5pp)   ⟹ 기본값 OFF · 성능 주장 없음
라우터 정확도가 우연 수준           ⟹ 분할이 학습 가능하지 않다. 분할 원칙 자체를 폐기.
C ≤ A                              ⟹ 검색이 이긴다. 상주 전문가의 근거 없음.
```

부차(보고하되 주를 대체하지 않음): 토큰·지연·모델 호출 수, 라우터 오배분 시 손실.

## 5. 이 실험이 답하지 못하는 것 (미리 적는다)

- **발산의 유용성은 여기서 안 나온다.** C가 이겨도 그건 *커버리지*의 값이지
  *다양성*의 값이 아니다. 두 기제를 섞어 해석하지 않는다.
- **일반화 단위는 우리 코퍼스다.** 다른 코퍼스에서 층 분할이 통한다는 주장은 하지 않는다.
- **모델은 고정한다.** 팔 간 모델을 바꾸면 그건 모델 실험이지 harness 실험이 아니다.

## 6. 즉시 정지

최적화기가 오라클/생성기에 영향을 줄 수 있는 경로 발견 · 프라이버시 경계 문서가 코퍼스에
포함됨(`_from_desktop`/`dain`/`minyoung`/`.vault`) · 팔 간 모델·예산 불일치.

---
*전제 측정(2026-08-18): `/data/jaewon/aios/index/aios.db` fulltext 5,065문서 41,014,460자
= 약 11,718,417 토큰, 200k context의 58.6배. 층별 최대 `docs` 3.6M 토큰.
근거: `docs/AIOS_WHY_2026-08-18.md` §1(기제 구별), `experiments/phase5g/G5_RESULTS.md`(투표형 사망),
`docs/AIOS_COPYNESS_GATE_2026-08-18.md` §5(G1의 출처), `docs/AIOS_RESIDUAL_DECISION_VALUE_2026-08-16.md` §4(c).
계측 규율: `scripts/aios_instrument.py`.*

## Errata (append-only)

**E1 · 2026-08-18 — G0가 첫 실행에서 FAIL했고, 그것이 §0의 가정 하나를 고쳤다.**
(실행 전 게이트이므로 데이터를 본 뒤의 정지규칙 교체가 아니다. 문제 풀의 사전 수정이다.)

첫 생성 풀 200문항: 증거 중앙값 **34,088 토큰**, context 초과 **25.5%뿐**. ⟹ **G0 FAIL.**

> **코퍼스가 context의 58.6배라는 사실은 개별 질문의 증거가 context를 넘는다는 뜻이 아니다.**
> 검색이 정확히 그 격차를 메우는 기제이고, **arm A가 이미 그걸 갖고 있다.**

§0은 "코퍼스가 크다 ⟹ 단일 agent가 못 본다"로 논증했는데, 그 함의는 **조회형 질문에는
성립하지 않는다.** 분할이 값을 가질 수 있는 곳은 **검색으로 좁힐 수 없는 곳** —
소수 문서의 조회가 아니라 **inbound 집합 전체에 대한 집계**다.

생성기를 그에 맞게 좁혔다(대상의 inbound 증거 합 > 1 context를 **구성 조건**으로).
재측정: 증거 중앙값 **304,175 토큰**, 초과 **200/200**, 다층 **200/200**. **G0 PASS.**

**대가를 적어둔다.** 좁히자 G1 최빈값이 33.0% → **51.0%**로 올라갔다(문턱 60%).
가설에 가장 부합하는 질문일수록 최대 층(`.aios`)에 쏠린다. **더 좁히면 G1이 깨진다** —
G0와 G1이 반대 방향으로 당기는 것이 이 설계의 실제 제약이며, 결과 해석 시 이 사실을 함께 읽는다.

**E2 · 2026-08-18 — 배관 점검 결과, 그리고 그것이 결과가 아닌 이유.**
`scripts/aios_m3_arms.py`로 arm A·C의 **경로**를 돌렸다(같은 시드, 같은 200문항 풀).

```
arm A  0.910   context로 194/200(97.0%) 잘림
arm C  1.000
```

**이 숫자를 성능으로 인용하지 않는다.** 두 가지 결함이 있다.

1. **arm C의 집계가 정답의 정의다.** 정답을 "출처들의 최빈 층"으로 정의해놓고 arm C가
   파티션별 출처 수를 세게 했으니, 만점은 발견이 아니라 **항등식**이다. M1에서 arm D를
   `task_id`로 인덱싱해 사후 장부로 만든 것과 같은 과다.
2. **에이전트가 없다.** 두 팔 다 기계적 계산이고 모델이 읽거나 판단하지 않았다.

⟹ 서는 것은 **배관뿐**이다: 라우터가 층으로 주소를 찾고(`mesh.who_knows`), 스포너가
입장 게이트를 통과해 7개 전문가를 띄우고, 단일 context가 실제로 97%에서 잘린다는 것.
마지막 항목은 실험의 전제(§0)를 독립적으로 확인해준다.

**실험이 되려면**: 정답이 읽고 판단해야만 나오는 것이어야 하고, 어느 팔도 정답을 정의하는
집계를 손에 쥐면 안 된다. arm B는 여전히 미구현이며(§5.2 — C와 무관한 구현자),
`compare`는 B 없이 C-vs-A를 결과로 보고하기를 **거부**한다.

**E3 · 2026-08-19 — 구조적 affinity. estimand를 더 선명한 것으로 교체한다.**
(실행 전 수정.)

독립 구현자(`myworld_computation`)의 지적: 세 과제유형의 정답은 본질적으로 layer/provenance
**구조**에 관한 것이므로, E2가 지적한 기계적 항등식을 real agent가 제거해도
**질문이 layer축이면 layer로 분할한 C가 유리**하다는 편향이 남는다 —
A1 `observable-competence ceiling`의 거울상.

⟹ **공정한 B는 C의 라우터가 쓰는 것과 동일한 layer 구조를 압축해 context에 상주시킬 수
있어야 한다.** 산문 요약이 아니라 **graph-structure-preserving digest**:
target별 inbound-by-layer counts · supersede edges · contradiction pairs.

그러면 실험 질문이 이렇게 선명해진다:

```
digest가 한 context에 들어가 답한다     ⟹ C 무이득. 정직한 null
digest 자체가 context를 초과한다        ⟹ partition이 값을 EARN. 정직한 positive
```

이것은 원래 §1의 대비보다 나은 estimand이므로 **채택한다.** B가 digest를 최대한
compact-yet-complete하게 지어야 진짜 시험이 되며, 그 책임은 구현자에게 있다.

그리고 구현자가 부차 추천으로 낸 것을 **필수로 올린다**: **정답이 층 자체가 아닌 질문
(`kind: non_layer`)을 풀에 섞어** E2가 지적한 항등식을 완전히 차단한다.

**미리 적어두는 해석 규약**: B가 이겨도 실패가 아니다. *"구조 인식 압축이 분할을 이긴다"*는
쓸모 있는 발견이고, 어느 쪽도 C 패배를 재앙으로 취급하지 않는다.

**E4 · 2026-08-19 — balance 게이트 추가, 그리고 동결한 분할 원칙이 이 코퍼스에서 퇴화한다.**
(실행 전. 독립 구현자가 데이터를 보기 전에 구조를 재서 제기했고, 내가 독립 재현했다.)

## 측정 (두 기질에서 독립 계산, 결론 일치)

```
전체 structural digest      6.30× context   (구현자 추정 6.76× — 표현 차이, 결론 동일)
.aios 단독                  5.60× context   ← 자기 파티션에도 안 들어감
.aios 제외 12층 합계        0.70× context   ← 한 context에 전부 들어감
질량 집중도                 .aios가 target의 87.1%
```

⟹ **동결한 `layer` 분할은 B와 C를 가르지 못한다.** 비-.aios 질문에서는 B가 전 구조를
resident로 들고 있으므로 분할이 무의미하고, .aios 질문에서는 C의 .aios 전문가도 같은
context 벽에 부딪히므로 분할이 구제하지 못한다. 게다가 E1의 G1 최빈(51% `.aios`)이
말하듯 **가설 정합 질문이 바로 그곳에 쏠린다.**

## 새 게이트 (G0·G1과 같은 지위)

> **G2-balance: 동결된 어느 파티션도 1 context를 초과하지 않을 것.**

이것이 §1의 *"결과를 보고 분할을 바꾸면 튜닝"*과 충돌하지 않는 이유: G0·G1과 마찬가지로
**결과가 아니라 설계의 식별 가능성**에 대한 사전 사실이다. 결과가 틀리게 나와서 바꾸면 튜닝이고,
어떤 결과도 보기 전에 분할이 퇴화했음을 알아서 바꾸면 설계다. 판별식은 시점이며, 이 수정은
데이터 이전이다.

## 판정: sub-partition으로 구제되지 않는다

`.aios`를 쪼개 봤다.
```
kind 기준          2조각 — receipt 44,116 (5.60×) + contract 3
경로 2단계 기준    65조각 — myworld/.aios/invocations 41,918 (5.33×) + 나머지 전부 <0.13×
```
**질량의 95%가 `.aios/invocations` 한 곳**이고, 그것은 서로 구별되는 지식이 아니라
**같은 형태의 호출 영수증 4만 건**이다. 어떤 sub-rule로도 1 context 아래로 내려가지 않으며,
내려가게 만들려면 영수증을 임의 등분하는 수밖에 없는데 그건 분할 원칙이 아니라 샤딩이다.

⟹ 구현자가 제시한 (a) sub-partition은 **이 코퍼스에서 불가능**하다.

## 채택: (b) 재스코프 — 단, 정직한 형태로

`.aios/invocations`를 **코퍼스에서 제외**하고 남은 것으로 다시 잰다. 제외 근거는 결과가 아니라
성질이다: 영수증 로그는 회수 대상 지식이 아니라 실행 장부이며, **M1이 죽은 이유와 같은 범주**다
(비트를 가진 필드가 전부 실험 장부였다).

제외 후 예상 규모는 0.70×~1.0× context로, 그러면 **전체가 한 context에 들어간다** — 즉
이 코퍼스에서는 **분할이 값을 가질 수 없다**는 것이 pre-data 결론이다.

**이것을 정직한 NO-GO로 기록한다.** M3는 이 코퍼스에서 돌리지 않는다.
분할형 사회를 시험하려면 **한 context를 초과하면서 구조적으로 균형 잡힌 코퍼스**가 필요하고,
지금 진행 중인 논문 그래프(8층 스택, hop 확장)가 그 후보다 — 층별 질량이 로그 하나에
쏠리지 않기 때문이다. 재실행 조건: **어느 단일 파티션도 전체 digest의 40%를 넘지 않을 것.**

이 판정은 A1 `observable-competence ceiling`의 M3판이다 — **기제가 나쁜 것이 아니라
기질이 그 기제가 요구하는 shape을 갖지 않았다.**

**E5 · 2026-08-19 — E4의 마지막 문장은 명명이었지 발견이 아니었다. 내린다.**

E4는 이렇게 끝났다: *"이 판정은 A1 observable-competence ceiling의 M3판이다 — 기제가
나쁜 것이 아니라 기질이 그 기제가 요구하는 shape을 갖지 않았다."* 그 틀은 **내가 붙였고**,
독립 구현자가 그 위에 `substrate-shape void`를 민팅하려다 keystone 규율대로 비-Claude
목소리(deepseek-r1, council)에 반증을 걸었다. **전면 환원됐고 잔차가 없다.**

```
"real-active yet decision-void"  → sufficient context / degenerate feature
                                    (Sufficient Context, ICLR 2025, arXiv:2411.06037 — 확인함)
"한 파티션에 89% 집중"           → degenerate design matrix · class imbalance
                                    (split-balance 통계가 이미 잡는다)
"shape-gate"                     → preregistered moderation analysis (Nosek 2018)
                                    + conditional inductive bias (Baxter 2000)
```

**죽은 것과 서는 것을 가른다:**

| | |
|---|---|
| **선다** — M3 NO-GO | **측정**이다. 두 기질에서 독립 재현됐고(6.30× vs 6.76×, 87.1% vs 89%) executable하다. 이 반증에 흔들리지 않는다 |
| **선다** — G2-balance 게이트 | **빌려온 건전한 관행**이다. 계속 쓰되 **발명이라 주장하지 않고** 위 선행연구를 인용한다 |
| **죽는다** — "새 개념" 옷 | `observable-competence ceiling의 M3판`이라는 틀. keystone처럼 부르지 않는다 |

기록해둘 것: 오늘 내 틀이 외부 목소리에 죽은 것이 **두 번째**다(첫째는 엔트로피→support 추론).
둘 다 내 것이 아니라 **다른 가중치**가 잡았고, 이것이 어제 적은 셋째 축(정확성 = 이종 + 실행
가능한 검증)이 노동 분배와 다른 축이라는 주장의 실증이다.
